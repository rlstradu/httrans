{
  "name": "poanda",
  "version": "2.0.0",
  "private": true,
  "description": "Poanda — editor de traducción para más de veinte formatos (PO, Word, Excel, XLIFF, Markdown y demás), con glosario TBX y memoria de traducción TMX. Parte de PandaTools by HTTrans.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/publicar-compilado.mjs",
    "preview": "npm run build && npx --yes serve .. -l 5173",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "fake-indexeddb": "^6.2.5",
    "vite": "^5.4.21",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "dexie": "^4.4.5",
    "fflate": "^0.8.3"
  }
}
